extends layout

block content
  h1= title
  ul
    each author in author_list
      //- pre=JSON.stringify(author, null, 2)
      li
        a(href=author.url) #{author.name} (#{author.lifespan})

    else
      li There are no authors.
